{
  "name": "holusion-doc",
  "version": "0.0.1",
  "private": true,
  "main": "app.js",
  "scripts": {
    "test": "mocha --slow 1500 --timeout 10000 ./scripts/runner.js",
    "jenkins_test": "JUNIT_REPORT_PATH=report.xml SELENIUM_PROMISE_MANAGER=0 mocha ./scripts/runner.js --reporter mocha-jenkins-reporter"
  },
  "os": [
    "linux"
  ],
  "dependencies": {
    "bower": "*"
  },
  "devDependencies": {
    "chai": "^4.0.2",
    "finalhandler": "^1.1.0",
    "mocha": "^3.4.2",
    "mocha-jenkins-reporter": "0.3.9",
    "puppeteer": "^0.13.0",
    "request": "^2.81.0",
    "serve-static": "^1.13.1"
  }
}
